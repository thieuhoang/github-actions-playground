name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: windows-latest
    steps:
      - name: list file
        run: |
          & cmd.exe dir
          tar.exe -xvf Libraries.zip
          dir "C:\Program Files (x86)\Microsoft SDKs"
          dir "C:\Program Files (x86)"